---
title: "Fold Animation Test"
date: 2026-04-09
draft: false
tags: [test]
excerpt: "Temporary test page for the paper-fold scroll animation on extended code blocks."
---

This is a temporary test page to preview the paper-fold scroll animation on code blocks that extend beyond the central column. Scroll down to see the effect.

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.

Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum. Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium.

## Short code block (should not extend)

This block stays within the column:

```typescript
const greeting = "hello world";
console.log(greeting);
```

## Long code block (should extend and fold)

This block has long lines that extend beyond the central column:

```typescript
// This is a very long line of code that should definitely extend beyond the central column boundary and trigger the paper-fold animation effect when scrolling
export async function fetchUserProfileWithAllRelatedDataAndPermissions(userId: string, organizationId: string, includeArchived: boolean = false): Promise<UserProfileWithRelatedData> {
  const userProfile = await database.query("SELECT u.*, o.name as org_name, o.settings as org_settings FROM users u JOIN organizations o ON u.org_id = o.id WHERE u.id = $1 AND o.id = $2", [userId, organizationId]);

  const permissions = await database.query("SELECT p.name, p.description, p.scope, r.name as role_name FROM permissions p JOIN role_permissions rp ON p.id = rp.permission_id JOIN roles r ON rp.role_id = r.id WHERE r.id IN (SELECT role_id FROM user_roles WHERE user_id = $1)", [userId]);

  const activityLog = await database.query("SELECT a.action, a.target_type, a.target_id, a.metadata, a.created_at FROM activity_log a WHERE a.user_id = $1 AND a.org_id = $2 ORDER BY a.created_at DESC LIMIT 100", [userId, organizationId]);

  return { profile: userProfile.rows[0], permissions: permissions.rows, recentActivity: activityLog.rows, fetchedAt: new Date().toISOString(), includesArchived: includeArchived };
}
```

More content between code blocks. Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui ratione voluptatem sequi nesciunt. Neque porro quisquam est, qui dolorem ipsum quia dolor sit amet, consectetur, adipisci velit.

## Another long code block

A second extended block to test the fold animation repeating:

```css
/* This is an intentionally long CSS rule to test the fold animation. It contains many properties and a very long selector that extends well beyond the column boundary */
.site-container .content-inner .article-content .expressive-code pre code .line .token.string.template-expression .token.function { color: var(--syntax-function); font-weight: 600; text-decoration: underline; text-decoration-color: rgba(184, 114, 42, 0.3); transition: color 0.2s ease, text-decoration-color 0.2s ease, background-color 0.2s ease; }

.site-container .content-inner .article-content .expressive-code pre code .line .token.keyword.control-flow { color: var(--syntax-keyword); font-style: italic; letter-spacing: 0.02em; }

@media (min-width: 1200px) and (prefers-color-scheme: dark) and (prefers-contrast: more) and (prefers-reduced-motion: no-preference) { .site-container .content-inner .article-content { font-size: clamp(1rem, 0.5vw + 0.875rem, 1.25rem); line-height: 1.75; } }
```

And some trailing content after the code blocks. Ut enim ad minima veniam, quis nostrum exercitationem ullam corporis suscipit laboriosam, nisi ut aliquid ex ea commodi consequatur.

## One more with mixed lengths

```javascript
// Short lines mixed with very long ones
import { createServer } from "node:http";

const server = createServer((req, res) => {
  // This is a normal length line
  const url = new URL(req.url, `http://${req.headers.host}`);

  // This next line is intentionally very long to trigger the overflow and test the fold animation at different widths of extension beyond the column boundary
  const responsePayload = JSON.stringify({ status: "ok", timestamp: Date.now(), requestId: crypto.randomUUID(), path: url.pathname, query: Object.fromEntries(url.searchParams), headers: Object.fromEntries(Object.entries(req.headers)) });

  res.writeHead(200, { "Content-Type": "application/json", "X-Request-Id": crypto.randomUUID(), "Cache-Control": "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" });
  res.end(responsePayload);
});

server.listen(3000);
```

End of test page.
