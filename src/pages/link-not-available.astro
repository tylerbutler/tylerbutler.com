---
import BaseLayout from "../layouts/BaseLayout.astro";

const url = Astro.url.searchParams.get("url") || "Unknown URL";
---

<BaseLayout
  title="Link Not Available - Tyler Butler"
  description="This link is no longer available"
  headerSize="small"
>
  <article class="link-not-available">
    <div class="icon">🔗💔</div>
    <h1>Link Not Available</h1>

    <p class="explanation">
      The link you're trying to access is no longer available. This is likely
      due to:
    </p>

    <ul class="reasons">
      <li>Content migration from an older version of this site</li>
      <li>External resources that have been removed or relocated</li>
      <li>Deprecated URLs from previous site architectures</li>
    </ul>

    <div class="url-box">
      <p class="url-label">Original URL:</p>
      <code class="broken-url">{url}</code>
    </div>

    <div class="actions">
      <a href="/" class="button primary">Go to Home</a>
      <a href="/articles" class="button secondary">Browse Articles</a>
    </div>
  </article>
</BaseLayout>

<style>
  .link-not-available {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
    padding: 2em 1em;
  }

  .icon {
    font-size: 4em;
    margin-bottom: 0.5em;
    opacity: 0.8;
  }

  h1 {
    font-family: adelle, Georgia, serif;
    font-size: clamp(32px, 5vw, 42px);
    font-weight: 300;
    line-height: 1.2;
    color: var(--text);
    margin: 0 0 1em 0;
  }

  .explanation {
    font-size: 16px;
    line-height: 1.6;
    color: #666;
    margin-bottom: 1em;
  }

  .reasons {
    text-align: left;
    max-width: 500px;
    margin: 0 auto 2em;
    font-size: 14px;
    line-height: 1.6;
  }

  .reasons li {
    margin-bottom: 0.5em;
  }

  .url-box {
    background: #f5f5f5;
    border: 1px solid #ddd;
    border-radius: 6px;
    padding: 1.5em;
    margin: 2em 0;
  }

  .url-label {
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: #666;
    margin: 0 0 0.5em 0;
  }

  .broken-url {
    display: block;
    background: white;
    border: 1px solid #ddd;
    border-radius: 4px;
    padding: 0.75em;
    font-family: "Pragmata Pro Mono", "Courier New", monospace;
    font-size: 13px;
    color: #333;
    word-break: break-all;
    text-align: left;
  }

  .actions {
    display: flex;
    gap: 1em;
    justify-content: center;
    margin-top: 2em;
  }

  .button {
    display: inline-block;
    padding: 12px 24px;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.2s ease;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .button.primary {
    background: var(--accent);
    color: white;
    border: 2px solid var(--accent);
  }

  .button.primary:hover {
    background: transparent;
    color: var(--accent);
  }

  .button.secondary {
    background: transparent;
    color: var(--accent);
    border: 2px solid var(--accent);
  }

  .button.secondary:hover {
    background: var(--accent);
    color: white;
  }

  @media (max-width: 600px) {
    .actions {
      flex-direction: column;
    }

    .button {
      width: 100%;
    }
  }
</style>
