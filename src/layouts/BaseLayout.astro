---
import BrokenLinkTooltip from "../components/BrokenLinkTooltip.astro";
import Footer from "../components/Footer.astro";
import FootnoteScript from "../components/FootnotesScript.astro";
import Header from "../components/Header.astro";
import Tinylytics from "../components/Tinylytics.astro";
import "../lib/styles/global.css";
import "../styles/fonts.css";

export interface Props {
  title?: string;
  description?: string;
  headerSize?: "large" | "small";
  canonicalUrl?: string;
}

const {
  title = "Tyler Butler",
  description = "Personal website and blog of Tyler Butler",
  headerSize = "large",
  canonicalUrl,
} = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="generator" content={Astro.generator}>
    <title>{title}</title>
    <meta name="description" content={description}>
    {canonicalUrl && <link rel="canonical" href={canonicalUrl} />}
    <link rel="stylesheet" href="https://use.typekit.net/zsx5vsn.css">
    <link
      rel="preload"
      href="/fonts/PragmataPro_Mono_R_liga_0902.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    >
    <!-- IndieAuth -->
    <link rel="authorization_endpoint" href="https://indieauth.com/auth" />
    <link rel="token_endpoint" href="https://tokens.indieauth.com/token" />
    <!-- IndieWeb endpoints -->
    <link rel="micropub" href="https://tylerbutler.com/micropub" />
    <link rel="webmention" href="https://webmention.io/tylerbutler.com/webmention" />
    <!-- rel="me" for IndieAuth identity verification -->
    <link rel="me" href="https://github.com/tylerbutler" />
    <link rel="me" href="https://hachyderm.io/@tyler" />
    <link rel="me" href="mailto:tyler@tylerbutler.com" />
    <!-- Analytics -->
    <Tinylytics embedCode="7vP3rWZsVnwMyLpSbFxs" kudos="custom" hits />
  </head>
  <body>
    <div class="site-container">
      <Header size={headerSize}/>
      <main class="content-section" data-pagefind-body>
        <div class="content-inner">
          <slot/>
        </div>
      </main>
      <Footer/>
    </div>
    <FootnoteScript/>
    <BrokenLinkTooltip/>
  </body>
</html>
